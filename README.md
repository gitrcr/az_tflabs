# Proyecto Terraform Azure

## modules

## environments

### dev

init_terraform.ps1      #terraform init + fmt in subdirs

python3 dashboard.py    #select env and actions: validate + apply / destroy

